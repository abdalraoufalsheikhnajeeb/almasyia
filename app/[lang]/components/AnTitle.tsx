
const AnTitle = ({ title }: { title: string }) => {
  return (
    
      <h2 className="lg:text-7xl font-bold mb-8 text-accent text-center text-5xl">
        {title}
      </h2>
  
  );
};

export default AnTitle;
